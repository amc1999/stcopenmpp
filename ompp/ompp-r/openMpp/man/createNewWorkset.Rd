% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/createWorkset.R
\name{createNewWorkset}
\alias{createNewWorkset}
\title{Create a new working set of model parameters (internal use)}
\usage{
createNewWorkset(dbCon, defRs, i_isRunBased, i_baseRunId, i_setDef, ...)
}
\arguments{
\item{dbCon}{Database connection.}

\item{defRs}{Model definition: database rows describing model input parameters and output tables.}

\item{i_isRunBased}{Logical; if TRUE, use parameters from a base run (\code{i_baseRunId}).
If FALSE, all parameters must be supplied via \code{...}.}

\item{i_baseRunId}{ID of the model run results to copy parameters from (used if \code{i_isRunBased = TRUE}).}

\item{i_setDef}{Optional workset definition as a data frame with columns:
\describe{
\item{name}{Working set name.}
\item{lang}{Language code.}
\item{descr}{Workset description.}
\item{note}{Optional workset notes.}
}}

\item{...}{List of parameter values and optional value notes. Each element is a list with components:
\describe{
\item{name}{Parameter name (character).}
\item{subCount}{Optional: number of parameter sub-values (default: 1).}
\item{defaultSubId}{Optional: default sub-value ID (default: 0).}
\item{subId}{Optional: sub-value ID (default: 0).}
\item{value}{Parameter value: scalar, vector, or data frame.
If a data frame, it must have columns \code{dim0}, \code{dim1}, …, \code{value},
and each column length must equal the product of dimension sizes.}
\item{txt}{Optional workset parameter text: data frame with columns \code{lang} (language code) and \code{note} (value notes).}
}}
}
\value{
Integer ID of the newly created workset. Returns \verb{<= 0} on error.
}
\description{
\strong{Internal function. Must be called within a transaction scope.}
Creates a new working set of model parameters and returns its ID.
}
\details{
This function creates a new workset of model parameters.
If \code{i_isRunBased = TRUE}, parameters are copied from an existing run (\code{i_baseRunId}).
Otherwise, all parameters must be supplied via \code{...}.

Each workset has a unique set ID (positive integer) and can optionally have a name.
Parameters may have multiple sub-values; defaults are \code{subCount = 1}, \code{subId = 0}, \code{defaultSubId = 0}.

\strong{Note:} This function is internal and must be executed in a transaction. Do not call directly from outside code unless you understand transaction scope.
}
\keyword{internal}
