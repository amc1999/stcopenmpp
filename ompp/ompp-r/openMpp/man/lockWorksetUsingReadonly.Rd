% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/setReadonlyWorkset.R
\name{lockWorksetUsingReadonly}
\alias{lockWorksetUsingReadonly}
\title{Set read-only flag for a workset (internal)}
\usage{
lockWorksetUsingReadonly(
  dbCon,
  i_defRs,
  i_isSetId,
  i_setId,
  i_isAddValue,
  i_readonlyValue
)
}
\arguments{
\item{dbCon}{Database connection.}

\item{i_defRs}{Model definition database rows.}

\item{i_isSetId}{Logical; if \code{TRUE}, use \code{i_setId} to identify workset, otherwise use default workset by model ID.}

\item{i_setId}{Workset ID to update (used if \code{i_isSetId = TRUE}).}

\item{i_isAddValue}{Logical; if \code{TRUE}, add \code{i_readonlyValue} to the existing \code{is_readonly} value, otherwise set it directly.}

\item{i_readonlyValue}{Integer value to assign or add to \code{is_readonly}.}
}
\value{
Data frame of \code{workset_lst} rows with updated \code{is_readonly} values.
}
\description{
\strong{Internal use only. Must be executed within a database transaction.}
Set or update the read-only flag for a workset, either by workset ID or by model ID.
Returns the corresponding rows from \code{workset_lst} after the update.
}
\details{
This function updates the \code{is_readonly} flag in the \code{workset_lst} table.
Behavior depends on \code{i_isAddValue}:
\itemize{
\item If \code{i_isAddValue = TRUE}, the current \code{is_readonly} is incremented by \code{i_readonlyValue}.
\item If \code{i_isAddValue = FALSE}, \code{is_readonly} is set directly to \code{i_readonlyValue}.
}

The function returns a data frame with the updated rows:
\itemize{
\item \code{set_id} — workset ID
\item \code{model_id} — model ID
\item \code{is_readonly} — updated read-only flag
}
}
\note{
Must be executed inside a database transaction.
}
\keyword{internal}
